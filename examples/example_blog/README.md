# Leptos Fetch Example with Axum

This example creates some basic resources and queries, useful for testing leptos-fetch and it's devtools.

## Quick Start

Run `cargo leptos watch` to run this example.
